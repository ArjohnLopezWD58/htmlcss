<script>
const list = document.querySelectorAll('.lst');
function activelink(){
    list.forEach((item)) =>
    item.classlist.remove('active');
    this.classlist.add(active);
}
list.forEach((item) =>
item.addEventlistener('click',))
</script>